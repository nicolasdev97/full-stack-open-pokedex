name: Hello World

on:
  push:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print hello world
        run: echo "Hello World!"

      - name: Print date
        run: date

      - name: List directory contents
        run: ls -l
